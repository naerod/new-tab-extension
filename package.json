{
  "name": "newtab-extension",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Chrome MV3 new tab extension — dev/test tooling only (not shipped).",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
